{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm ci --ignore-scripts && npm test"
  },
  "deploy": {
    "startCommand": "npm start",
    "cronSchedule": "0 */6 * * *",
    "restartPolicyType": "NEVER"
  }
}
