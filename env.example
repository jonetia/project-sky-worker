# Copy this to .env for local testing only -- never commit real values.
# In Railway, set these under the service's Variables tab instead.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
CELESTRAK_GROUPS=active
